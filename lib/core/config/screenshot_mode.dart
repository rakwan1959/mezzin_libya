/// وضع لقطات الشاشة — يُفعَّل وقت البناء فقط:
///
///     flutter drive ... --dart-define=SCREENSHOT_MODE=true
///
/// الغرض: على محاكي CI لا يوجد أحد يضغط «السماح» في حوارات صلاحيات نظام iOS،
/// وحوار الإشعارات يظهر داخل `NotificationService.init()` **قبل** `runApp`،
/// فيبقى الإقلاع معلّقاً بانتظار ردّ المستخدم ولا تظهر الواجهة إطلاقاً.
/// عند تفعيل هذا العلم لا تُطلب الصلاحيات، فيُقلع التطبيق فوراً وتُلتقط اللقطات.
///
/// القيمة `false` افتراضياً، فلا يتغيّر سلوك التطبيق الحقيقي على المتجر.
const bool kScreenshotMode = bool.fromEnvironment('SCREENSHOT_MODE');
